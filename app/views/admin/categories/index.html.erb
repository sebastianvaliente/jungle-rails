<header class="page-header">
  <h1>Admin &raquo; Categories</h1>
</header>

<div class="well">
  <%= link_to '+ New Categories', [:new, :admin, :category], class: 'btn btn-info' %>
</div>

<% Category.all.each do |c| %>
  <h3><%= link_to c.name, c.name %></h3>
<% end %>
